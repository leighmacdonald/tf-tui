syntax = "proto3";
package proto;
option go_package = "./proto";

message GetRequest {
    string key = 1;
}

message GetResponse {
    string key = 1;
}

service Players {
    rpc Get(GetRequest) returns (GetResponse);
}
