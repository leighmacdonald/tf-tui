# User Plugin


## Building Protobuf Definitions 

Install binary from https://github.com/knqyf263/go-plugin into `$PATH`.

Make desired changes to `plugins.proto`

Run `make` to generate the new definitions & compile the example plugin.
